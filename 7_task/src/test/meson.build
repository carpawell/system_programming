gtest = dependency('gtest', main: true)
test(
    'ln80cBufferTest',
    executable(
        'ln80cBufferTest',
        sources: ['ln80cBufferTest.cpp'],
        include_directories: src,
        dependencies: [gtest]
    )
)